services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "80:8080"
    volumes:
      - lofam-data:/data
    environment:
      - PORT=8080
      - DB_PATH=/data/lofam.db
      - STATIC_DIR=./static

volumes:
  lofam-data:
